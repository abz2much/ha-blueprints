# <Blueprint Name>

One or two sentences on what this catches/automates and why it's useful.

## What it does

1. Trigger...
2. Condition(s)...
3. Action(s)...

## Before you import

List anything that must exist first: helpers, other integrations, App
(add-on) dependencies, physical hardware. If there's nothing beyond stock
Home Assistant, say so explicitly, that's a selling point.

## Importing

1. Copy the raw GitHub URL for `<blueprint-name>.yaml`.
2. Settings → Automations & Scenes → Blueprints → Import Blueprint → paste
   the URL.
3. Create an automation from the blueprint and fill in your entities.

## Variants (if any)

If there's more than one version (e.g. a simple rule-based one and a more
advanced one with an external dependency), list them here with the
trade-off, so people can pick the right one for their setup.
